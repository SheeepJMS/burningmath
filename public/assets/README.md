# Assets

- **logo.png** 或 **logo.svg** — 导航栏左上角 logo。放入本目录（即 `public/assets/`），扩展名为 .svg 时在 `siteConfig.ts` 中将 `logoPath` 改为 `/assets/logo.svg`。
- **hero-medals.jpg** (or **resultsMedalImage**) — Used in the “Proven outcomes in Vancouver” section (right-side image). Copy from `Medal_Photo_multiuse.jpg` if you have it.
- **coach.png** — 教练肖像：Hero 右侧小卡片 + Meet the Coach 区块都用这张。**请把您的肖像图（西装、拿笔那张）保存为本目录下的 `coach.png`**，刷新页面即可显示。
- **report-1.png**, **report-2.png**, **report-3.png** — Platform report screenshots (placeholders until you upload).

Until these files exist, the site uses placeholder behavior; add the images here for production.
